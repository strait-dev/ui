---
"@strait/ui": patch
---

refactor: consume the new motion and z-index tokens across all components (floating surfaces enter at base and exit one step faster; dialogs use the slow step; every stacking context takes a z token)
