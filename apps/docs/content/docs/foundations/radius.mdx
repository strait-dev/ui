---
title: Radius
description: The corner-radius scale, all derived from a single base token.
---

Every radius derives from one base token, `--radius`. The scale
(`rounded-sm` → `rounded-4xl`) is computed as multiples of it, so rounding stays
proportional. Change `--radius` once to make the whole system rounder or
sharper.

<RadiusScale />
