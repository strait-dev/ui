---
title: Spacing
description: The spacing scale used for padding, margins, and gaps.
---

Spacing derives from a single base unit (`--spacing`, `0.25rem`). Tailwind's
numeric utilities (`p-4`, `gap-2`, `mt-8`) are multiples of it, so layout rhythm
stays consistent across components. A representative sample:

<SpacingScale />
