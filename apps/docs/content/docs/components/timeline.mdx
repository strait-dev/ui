---
title: Timeline
description: Stepped progress indicator with vertical and horizontal orientations, controlled active step, and configurable connector line styles.
---

<ComponentPreview name="timeline/demo" />

## Examples

### Color variants

The `variant` prop tints completed indicators and connectors with a semantic color — `primary`, `success`, `info`, or `warning`.

<ComponentPreview name="timeline/variants" />

### With icons

Pass an `icon` prop to `TimelineIndicator` to render a Hugeicon centered inside the indicator bubble.

<ComponentPreview name="timeline/with-icons" />

### Horizontal dotted roadmap

Combine `orientation="horizontal"` and `line="dotted"` for a milestone or roadmap view.

<ComponentPreview name="timeline/horizontal" />

## Reference

<PropsTable name="timeline" />
