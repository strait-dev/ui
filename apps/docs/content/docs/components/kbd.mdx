---
title: Kbd
description: Displays a keyboard key or shortcut combination.
---

<ComponentPreview name="kbd/demo" />

## Examples

### Keyboard chords

Wrap multiple `Kbd` keys in a `KbdGroup` to render a multi-key shortcut as a single inline unit.

<ComponentPreview name="kbd/chords" />

### Sizes

Three size presets — `sm`, `default`, and `lg` — scale the cap height, padding, and text.

<ComponentPreview name="kbd/sizes" />

### Inside a button

`Kbd` recolors automatically when placed inside a `Button` to remain legible against any button surface.

<ComponentPreview name="kbd/in-button" />

## Reference

<PropsTable name="kbd" />
