---
title: Password Input
description: A password field with an inline toggle button that reveals or masks the entered text, with an optional label and size axis.
---

<ComponentPreview name="password-input/demo" />

## Examples

### With built-in label

The `label` prop renders a linked `<label>` element above the field — no separate label component needed.

<ComponentPreview name="password-input/with-label" />

### Invalid state

Apply `aria-invalid` to show the destructive border; pair with a helper message for full context.

<ComponentPreview name="password-input/invalid" />

### Sizes

Use the `size` prop to match the field height to other inputs in the same form: `sm`, `default`, and `lg`.

<ComponentPreview name="password-input/sizes" />

## Reference

<PropsTable name="password-input" />
