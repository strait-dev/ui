---
title: Navigation Menu
description: A horizontal navigation bar with animated flyout content panels for rich dropdown navigation.
---

<ComponentPreview name="navigation-menu/demo" />

## Examples

### Plain links

Use `NavigationMenuLink` with `navigationMenuTriggerStyle()` to render a flat nav bar of direct links without dropdown panels; pass `active` to highlight the current page.

<ComponentPreview name="navigation-menu/plain-links" />

### Rich dropdown panel

Combine `NavigationMenuTrigger` with a `NavigationMenuContent` containing icon-annotated `NavigationMenuLink` items to build a "mega-menu" with descriptions.

<ComponentPreview name="navigation-menu/rich-panel" />

## Reference

<PropsTable name="navigation-menu" />
