---
title: Config Row
description: A single label/value row for settings panels and detail drawers with an optional icon, description, and trailing action.
---

<ComponentPreview name="config-row/demo" />

## Examples

### With icons

Add an `icon` to reinforce the label's meaning at a glance.

<ComponentPreview name="config-row/with-icons" />

### With description

Use `description` to add a muted secondary line below the label for extra context.

<ComponentPreview name="config-row/with-description" />

### In a card

Compose multiple `ConfigRow` items inside a `Card` with `divide-y` to build a bordered settings panel.

<ComponentPreview name="config-row/in-card" />

## Reference

<PropsTable name="config-row" />
