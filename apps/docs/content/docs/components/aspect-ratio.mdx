---
title: Aspect Ratio
description: Container that enforces a fixed width-to-height ratio on its content using a CSS custom property.
---

<ComponentPreview name="aspect-ratio/demo" />

## Examples

### Common ratios

A gallery of popular aspect ratios — `16:9`, `4:3`, and `1:1` — each labelled in the corner.

<ComponentPreview name="aspect-ratio/common-ratios" />

### Video embed

Wrap a responsive `<iframe>` in `AspectRatio` to lock a YouTube embed to `16:9`.

<ComponentPreview name="aspect-ratio/video-embed" />

### Loading placeholder

Combine `AspectRatio` with an `animate-pulse` background for a skeleton that holds the correct space while the image loads.

<ComponentPreview name="aspect-ratio/placeholder" />

## Reference

<PropsTable name="aspect-ratio" />
