---
title: Form
description: A thin wrapper around @tanstack/react-form that wires field context, label associations, validation states, and error messages automatically.
---

<ComponentPreview name="form/demo" />

## Examples

### Sign-up form

A complete sign-up form using `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, and `FormMessage` — demonstrating required fields, helper text, and submit-time validation.

<ComponentPreview name="form/sign-up" />

### Validation errors

Triggering validation shows destructive `FormMessage` beneath each field; `FormLabel` turns red automatically via the shared field context.

<ComponentPreview name="form/validation-errors" />

## Reference

<PropsTable name="form" />
