---
title: Context Menu
description: A right-click contextual menu that opens at the pointer position relative to a trigger area.
---

<ComponentPreview name="context-menu/demo" />

## Examples

### With submenu

Nest a `ContextMenuSub` + `ContextMenuSubTrigger` + `ContextMenuSubContent` to create hierarchical actions.

<ComponentPreview name="context-menu/with-submenu" />

### With checkboxes

Use `ContextMenuCheckboxItem` to let users toggle options directly from the context menu.

<ComponentPreview name="context-menu/with-checkboxes" />

### With radio group

Use `ContextMenuRadioGroup` + `ContextMenuRadioItem` for mutually exclusive single-select options.

<ComponentPreview name="context-menu/with-radio-group" />

## Reference

<PropsTable name="context-menu" />
