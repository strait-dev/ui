---
title: Progress
description: A determinate progress bar that displays a numeric completion percentage with optional label and value readout.
---

<ComponentPreview name="progress/demo" />

## Examples

### Variants

The `variant` prop tints the indicator with a semantic color token — useful for conveying status at a glance.

<ComponentPreview name="progress/variants" />

### With label and value

Add `ProgressLabel` and `ProgressValue` as children to render a header row above the track.

<ComponentPreview name="progress/with-label" />

### Sizes

Four track heights via the `size` prop: `xs`, `sm`, `default`, and `lg`.

<ComponentPreview name="progress/sizes" />

## Reference

<PropsTable name="progress" />
