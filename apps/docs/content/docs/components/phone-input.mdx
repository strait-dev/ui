---
title: Phone Input
description: An international phone number field composed of a searchable flag/country-code selector and a number input that formats as the user types.
---

<ComponentPreview name="phone-input/demo" />

## Examples

### Pre-filled value

Initialize the field with an E.164-formatted phone number to show how the country flag and number format together.

<ComponentPreview name="phone-input/with-value" />

### Default country

Set `defaultCountry` to pre-select a flag and dial code so users in a known region can start typing immediately.

<ComponentPreview name="phone-input/default-country" />

### Sizes

The `variant` prop controls the height of both the country button and the number input: `sm`, `default`, and `lg`.

<ComponentPreview name="phone-input/sizes" />

## Reference

<PropsTable name="phone-input" />
