---
title: Shell
description: A top-level page layout wrapper that applies consistent padding, gap, and flex-column structure to a page or route section.
---

<ComponentPreview name="shell/demo" />

## Playground

Adjust the props live and copy the generated code.

<Playground name="shell" />

## Examples

### Variants

Three layout presets side by side: `default` (left-aligned), `centered` (horizontally centred children), and `fluid` (full-width sections).

<ComponentPreview name="shell/variants" />

### Centered layout

The `centered` variant adds `items-center`, making it ideal for auth pages, empty states, and onboarding flows.

<ComponentPreview name="shell/centered" />

## Reference

<PropsTable name="shell" />
