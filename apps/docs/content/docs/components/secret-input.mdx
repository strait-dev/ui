---
title: Secret Input
description: A controlled text input for secrets and API keys, with optional reveal toggle and clipboard copy button.
---

<ComponentPreview name="secret-input/demo" />

## Usage

```tsx
"use client";

import { useState } from "react";
import { Label } from "@strait/ui/components/label";
import { SecretInput } from "@strait/ui/components/secret-input";

export function Example() {
  const [value, setValue] = useState("sk_live_51H8xAbCdEfGhIjKl");

  return (
    <div className="flex w-80 flex-col gap-1.5">
      <Label htmlFor="api-key">API key</Label>
      <SecretInput
        id="api-key"
        onChange={(e) => setValue(e.target.value)}
        placeholder="sk_live_…"
        value={value}
      />
    </div>
  );
}
```

## Reference

<PropsTable name="secret-input" />
