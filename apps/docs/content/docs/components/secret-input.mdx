---
title: Secret Input
description: A controlled text input for secrets and API keys, with optional reveal toggle and clipboard copy button.
---

<ComponentPreview name="secret-input/demo" />

## Examples

### Controlled

Wire `value` and `onChange` to manage the secret value from the parent; both trailing buttons remain fully functional.

<ComponentPreview name="secret-input/controlled" />

### Action variants

Toggle `revealable` and `copyable` independently to show only the actions your context requires.

<ComponentPreview name="secret-input/variants" />

## Reference

<PropsTable name="secret-input" />
