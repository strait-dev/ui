---
title: Spinner
description: An animated loading indicator built on the HugeIcons Loading03 glyph with accessible role and aria-label.
---

<ComponentPreview name="spinner/demo" />

## Playground

Adjust the props live and copy the generated code.

<Playground name="spinner" />

## Examples

### Sizes

Five dimension presets from `xs` through `xl` — pass `size` to scale the spinner without touching `className`.

<ComponentPreview name="spinner/sizes" />

### Colors

The spinner inherits `currentColor`, so any `text-*` utility changes its color.

<ComponentPreview name="spinner/colors" />

## Reference

<PropsTable name="spinner" />
