---
title: Popover
description: A non-modal floating panel anchored to a trigger element for rich interactive content.
---

<ComponentPreview name="popover/demo" />

## Examples

### With form

Embed a small form inside `PopoverContent` to collect user input without opening a full modal.

<ComponentPreview name="popover/with-form" />

### Sizes

`PopoverContent` accepts a `size` prop (`sm`, `default`, `lg`) to control the panel's padding and text density.

<ComponentPreview name="popover/sizes" />

## Reference

<PropsTable name="popover" />
