---
title: Notice Banner
description: A monolithic self-contained banner with built-in dismiss state, icon, title, and optional action slot.
---

<ComponentPreview name="notice-banner/demo" />

## Examples

### Variants

All four intent variants side by side — each auto-renders the matching icon and applies semantic color tokens.

<ComponentPreview name="notice-banner/variants" />

### Dismissible

Add `dismissible` to render an internal dismiss button; the component returns `null` once dismissed.

<ComponentPreview name="notice-banner/dismissible" />

### With action

Use the `action` slot with `NoticeBannerAction` to add a trailing call-to-action button or link.

<ComponentPreview name="notice-banner/with-action" />

## Reference

<PropsTable name="notice-banner" />
