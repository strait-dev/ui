---
title: Tree
description: Hierarchical tree view built on React Aria Components with keyboard navigation, single and multiple selection, and expand/collapse.
---

<ComponentPreview name="tree/demo" />

## Examples

### File tree

A multi-level file/folder tree with leading icons; pass `defaultExpandedKeys` to open specific branches on first render.

<ComponentPreview name="tree/file-tree" />

### Selectable

Set `selectionMode="multiple"` so users can pick several nodes at once, or `"single"` for exclusive selection.

<ComponentPreview name="tree/selectable" />

### Navigation menu

Use a single-selection tree as a hierarchical navigation panel for settings or sidebar menus.

<ComponentPreview name="tree/navigation" />

## Reference

<PropsTable name="tree" />
