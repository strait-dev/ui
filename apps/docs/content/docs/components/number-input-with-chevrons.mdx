---
title: Number Input With Chevrons
description: A number input with stacked up/down chevron buttons in a trailing column, supporting arbitrary Intl.NumberFormatOptions formatting.
---

<ComponentPreview name="number-input-with-chevrons/demo" />

## Playground

Adjust the props live and copy the generated code.

<Playground name="number-input-with-chevrons" />

## Examples

### Currency formatting

Pass `formatOptions` with `style: "currency"` to display values as formatted money while stepping in cents.

<ComponentPreview name="number-input-with-chevrons/currency" />

### Sizes

The `size` prop (`sm`, `default`, `lg`) controls the height of the input group to match surrounding form controls.

<ComponentPreview name="number-input-with-chevrons/sizes" />

### Disabled

The chevron buttons and input are both non-interactive when the field is disabled.

<ComponentPreview name="number-input-with-chevrons/disabled" />

## Reference

<PropsTable name="number-input-with-chevrons" />
