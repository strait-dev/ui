---
title: Number Input With Buttons
description: A stepper-style number input with flanking decrement and increment buttons on the left and right edges, built on React Aria NumberField.
---

<ComponentPreview name="number-input-with-buttons/demo" />

## Playground

Adjust the props live and copy the generated code.

<Playground name="number-input-with-buttons" />

## Examples

### Quantity stepper

A cart-style quantity picker with `min` and `max` bounds; the displayed count reflects each button press.

<ComponentPreview name="number-input-with-buttons/quantity" />

### Sizes

Use `size` to align the stepper with adjacent fields: `sm`, `default`, and `lg`.

<ComponentPreview name="number-input-with-buttons/sizes" />

### Disabled

Both the increment and decrement buttons are non-interactive when the field is disabled.

<ComponentPreview name="number-input-with-buttons/disabled" />

## Reference

<PropsTable name="number-input-with-buttons" />
